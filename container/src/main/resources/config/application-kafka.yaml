spring:
  kafka:
    # the single-node broker of development/docker-compose.yaml
    bootstrap-servers: localhost:9092

bpms-api:
  kafka:
    group-id-suffix: local
    topics:
      workflow: "workflows"
      user-task: "user-tasks"
      workflow-module: "modules"
