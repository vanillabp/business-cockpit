import React from 'react';
import { buildTimestamp, buildVersion } from '../../Form';

const TestForm3 = () => <div>TestForm3: { buildVersion } from { buildTimestamp.toLocaleString() }</div>

export default TestForm3;
