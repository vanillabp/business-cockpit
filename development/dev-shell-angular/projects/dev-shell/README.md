# dev-shell

The Angular library published as `@vanillabp/bc-dev-shell-angular-module`. It is built and released
by the Maven module above it, not with the Angular CLI directly, and
[its README](../../README.md) says what a change here costs.

```sh
ng build dev-shell     # into dist/
ng test dev-shell      # unit tests through Karma
```
