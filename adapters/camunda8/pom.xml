<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.vanillabp.businesscockpit</groupId>
        <artifactId>adapters</artifactId>
        <version>0.4.0</version>
    </parent>

    <artifactId>camunda8-spring-boot-adapter</artifactId>

    <name>Camunda8 Spring Boot Business Cockpit Adapter</name>
    <description>camunda8</description>
    <url>https://www.vanillabp.io/</url>

    <properties>
        <java.version>17</java.version>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <dependencies>
    <!--
      Spring Boot 4 split spring-boot-autoconfigure into per-technology modules. The auto-configuration
      classes used here now live in these artifacts rather than coming along with the starter.
    -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-kafka</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-hibernate</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-data-mongodb</artifactId>
    </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>adapters-commons</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <!--
              The Spring Boot 4 flavour of the starter, and on the same 8.8 line as
              vanillabp-camunda8-adapter. Two things were wrong before: the "-3-" artifact is built against
              Boot 3.5 and dragged that generation into this Boot 4.1 project, and 8.9.11 disagreed with the
              adapter's 8.8.x - Camunda only guarantees client and cluster compatibility at equal versions,
              so a workflow module must not be forced onto a newer client by the cockpit adapter.

              Beware the naming: on the 8.8 line "camunda-spring-boot-4-starter" is the Boot 4 variant and
              "camunda-spring-boot-3-starter" the Boot 3 one, whereas from 8.9 on the unqualified
              "camunda-spring-boot-starter" is the Boot 4 one. See the comment in
              vanillabp-camunda8-adapter/spring-boot/pom.xml.
            -->
            <groupId>io.camunda</groupId>
            <artifactId>camunda-spring-boot-4-starter</artifactId>
            <version>8.8.33</version>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-tx</artifactId>
        </dependency>

        <dependency>
            <groupId>jakarta.persistence</groupId>
            <artifactId>jakarta.persistence-api</artifactId>
            <version>3.1.0</version>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-orm</artifactId>
            <version>6.1.10</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.data</groupId>
            <artifactId>spring-data-jpa</artifactId>
            <version>3.4.2</version>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.data</groupId>
            <artifactId>spring-data-mongodb</artifactId>
            <version>4.4.2</version>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.retry</groupId>
            <artifactId>spring-retry</artifactId>
            <version>2.0.8</version>
        </dependency>

        <dependency>
            <groupId>com.phactum.zeebe</groupId>
            <artifactId>zeebe-kafka-exporter-serde</artifactId>
            <version>8.8.0</version>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
</dependencies>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>VanillaBP</id>
            <organization>Phactum Softwareentwicklung GmbH</organization>
            <organizationUrl>https://www.phactum.at</organizationUrl>
        </developer>
    </developers>
</project>
