name: Build

on:
  pull_request:
  workflow_dispatch:

# a group of its own, apart from the publish workflow: this build runs 'install'
# and uploads nothing, so it cannot overwrite an artifact a publish run wrote and
# has no reason to wait behind one. Pull-request builds still run one at a time
# among themselves, queued and never cancelled.
concurrency:
  group: business-cockpit-pr-build
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: temurin
          cache: maven
      - name: Configure npm authentication for GitHub Packages
        run: |
          echo "//npm.pkg.github.com/:_authToken=${{ secrets.GITHUB_TOKEN }}" > ~/.npmrc
          echo "@vanillabp:registry=https://npm.pkg.github.com/" >> ~/.npmrc
          echo "always-auth=true" >> ~/.npmrc
      - name: Build and test
        # 'install' alone: it runs through verify, which is where the aggregated coverage reports
        # are written, and it puts the reactor's artifacts into the local repository, which the
        # Spring Boot tests of the container read from. Adding 'verify' after it would run the
        # whole lifecycle twice.
        #
        # '-Pjava-install' leaves the npm modules out of the reactor. That is where most of the
        # build time goes, and while the backend is being refactored nothing outside this
        # repository reads the frontend packages. Remove the profile again as soon as the
        # frontend is worked on.
        run: |
          mvn --batch-mode --no-transfer-progress -s $GITHUB_WORKSPACE/.github/workflows/github-packages-settings.xml \
          -Dnpm.registry=https://npm.pkg.github.com/ \
          -Dskip.npm.publish-snapshot=true \
          -Pjava-install \
          install
        env:
          USER_NAME: ${{ secrets.VANILLABP_USER_NAME }}
          USER_TOKEN: ${{ secrets.VANILLABP_USER_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload test reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: surefire-reports
          path: '**/target/surefire-reports/**'
          retention-days: 7
