# Workflow provider API

What a workflow module may implement so that the cockpit can ask it something rather than only being
told. `server` is what a workflow module implements, `client` is what the cockpit asks with.

Built as part of the reactor build described in the [root README](../../README.md#building-it).
