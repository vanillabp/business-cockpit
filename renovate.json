{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["github>vanillabp/renovate-config"],
  "packageRules": [
    {
      "description": [
        "No npm updates while version 1.0 is being built. The cockpit is not going to ship a user interface for every combination of platform and component library. It ships one reference implementation and an instruction an agent follows to build the interface a project really needs, and that work sits far back in the backlog.",
        "Until it happens, every npm dependency in this repository belongs to code which is going to be rewritten or dropped: the packages under 'ui', the three development shells, the simulator and the webapp of the server. An update there is work on something nobody keeps, and the pull requests for it hid the Maven updates which do matter.",
        "Switch this rule off again once the reference interface has reached its final shape. The roadmap carries it as story 1303 in 'prompts-businesscockpit/ROADMAP.md'."
      ],
      "matchManagers": ["npm"],
      "enabled": false
    },
    {
      "description": [
        "The openapi-generator stays on 6.x. The TypeScript client of the GUI API is generated by it, and the pin in 'apis/official-gui-api/client/pom.xml' says why: 7.x emits instanceOf type guards which index an object type, and TypeScript 4.8.4 rejects that. Version 7.25.0 was offered and the frontend build failed on exactly those lines.",
        "No option of the generator changes it. The full list of 'typescript-fetch' options of 7.25.0 has nothing about the type guards, and 'typescriptThreePlus', which this repository sets today, is gone in 7.x. What the generator needs is a newer TypeScript, and 4.8.4 is still what 'ui/bc-shared', the react development shell and the react webapp of the simulator use.",
        "That upgrade belongs to the reference interface, which is story 1303 in 'prompts-businesscockpit/ROADMAP.md' and which the rule above holds npm for. Switch this rule off with that one, or drop it if the generated TypeScript client goes away with the interface.",
        "Only the two POMs which generate TypeScript are named here. The three which generate Java and Spring are on 7.x and stay on it: holding them back would buy nothing and cost every later fix."
      ],
      "matchPackageNames": ["org.openapitools:openapi-generator-maven-plugin"],
      "matchFileNames": ["business-cockpit/pom.xml", "apis/official-gui-api/client/pom.xml"],
      "allowedVersions": "<7.0.0"
    }
  ]
}
